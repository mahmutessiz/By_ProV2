<Window x:Class="By_ProV2.AlisIcmalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alış İcmal"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F9F9F9">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 🟦 Başlık -->
        <Border Background="#2C3E50" CornerRadius="6" Padding="12" Margin="0,0,0,15">
            <TextBlock Text="ALIŞ İCMAL"
                       Foreground="White"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- 🔍 Arama Paneli -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="Tarih Aralığı:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                <DatePicker x:Name="dpStartDate" Width="120" Height="25" Margin="0,0,10,0"/>
                <TextBlock Text="ile" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="dpEndDate" Width="120" Height="25" Margin="0,0,15,0"/>
                <TextBlock Text="Cari Kodu:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBox x:Name="txtCariKodu" Width="150" Height="25" Margin="0,0,15,0" KeyDown="TxtCariKodu_KeyDown"/>
                <Button x:Name="btnSorgula" Content="Sorgula" Width="100" Height="25" Margin="0,0,10,0" Click="BtnSorgula_Click"/>
                <Button x:Name="btnTumunuListele" Content="Tümünü Listele" Width="120" Height="25" Click="BtnTumunuListele_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- 📊 Sonuç Özet -->
        <Border Grid.Row="2" Background="#E8F4FD" CornerRadius="5" Margin="0,0,0,15" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="lblToplamSut" Text="Toplam Süt: 0" FontWeight="Bold" FontSize="14" Margin="0,0,20,0"/>
                <TextBlock x:Name="lblOrtalamaYag" Text="Ortalama Yağ: 0" FontWeight="Bold" FontSize="14" Margin="0,0,20,0"/>
                <TextBlock x:Name="lblOrtalamaProtein" Text="Ortalama Protein: 0" FontWeight="Bold" FontSize="14" Margin="0,0,20,0"/>
                <TextBlock x:Name="lblToplamKayit" Text="Toplam Kayıt: 0" FontWeight="Bold" FontSize="14"/>
            </StackPanel>
        </Border>

        <!-- 📋 Sonuç Listesi -->
        <DataGrid x:Name="dgAlisIcmal"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#F2F2F2"
                  HeadersVisibility="Column"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Standard">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih, StringFormat=dd.MM.yyyy}" Width="*"/>
                <DataGridTextColumn Header="Cari Kodu" Binding="{Binding CariKod}" Width="*"/>
                <DataGridTextColumn Header="Alınan Süt" Binding="{Binding Miktar, StringFormat=N2}" Width="*"/>
                <DataGridTextColumn Header="Yağ Oranı" Binding="{Binding Yag, StringFormat=N2}" Width="*"/>
                <DataGridTextColumn Header="Protein" Binding="{Binding Protein, StringFormat=N2}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>