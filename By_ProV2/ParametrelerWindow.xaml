<Window x:Class="By_ProV2.ParametrelerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parametreler" Height="700" Width="950"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Section Header -->
        <Label Grid.Row="0" Content="Parametreler" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <!-- Parameters Grid -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Oil Cutoff Parameter -->
            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,10,0">
                <Label Content="Yağ Kesinti Parametresi:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtYagKesinti" Height="25" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Örn. 3.60" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                </Grid>
            </StackPanel>

            <!-- Protein Parameter -->
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0,0,10,0">
                <Label Content="Protein Parametresi:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtProtein" Height="25" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Örn. 3.20" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                </Grid>
            </StackPanel>

            <!-- Price Per Dizem -->
            <StackPanel Grid.Column="2" Orientation="Vertical">
                <Label Content="Dizem Başına TL:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtDizemBasiTl" Height="25" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Örn. 0.28" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                </Grid>
            </StackPanel>
        </Grid>

        <!-- Donma Noktası Parameters Grid -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Donma Noktası Referans Değeri -->
            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,10,0">
                <Label Content="Donma Noktası Referans Değeri:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtDonmaNoktasiReferansDegeri" Height="25" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Örn. -0.51534" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                </Grid>
            </StackPanel>

            <!-- Donma Noktası Kesinti Alt Limit -->
            <StackPanel Grid.Column="1" Orientation="Vertical">
                <Label Content="Donma Noktası Kesinti Alt Limit:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtDonmaNoktasiKesintiAltLimit" Height="25" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Örn. -0.52500" VerticalAlignment="Center" Foreground="Gray" FontSize="10"/>
                </Grid>
            </StackPanel>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
            <Button x:Name="btnKaydet" Content="💾 Kaydet" Width="120" Height="35" Margin="0,0,10,0" Click="btnKaydet_Click" Background="#4CAF50" Foreground="White" FontWeight="SemiBold"/>
            <Button x:Name="btnVazgec" Content="❌ Vazgeç" Width="120" Height="35" Click="btnVazgec_Click" Background="#f44336" Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- History List Label -->
        <Label Grid.Row="6" Content="Parametre Geçmişi:" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- History List -->
        <DataGrid Grid.Row="7" x:Name="dgParametreler" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Margin="0,0,0,10" 
                  SelectionChanged="dgParametreler_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Yağ Kesinti" Binding="{Binding YagKesintiParametresi}" Width="*"/>
                <DataGridTextColumn Header="Protein" Binding="{Binding ProteinParametresi}" Width="*"/>
                <DataGridTextColumn Header="Dizem Başına TL" Binding="{Binding DizemBasiTl}" Width="*"/>
                <DataGridTextColumn Header="Donma Ref." Binding="{Binding DonmaNoktasiReferansDegeri}" Width="*"/>
                <DataGridTextColumn Header="Donma Alt Limit" Binding="{Binding DonmaNoktasiKesintiAltLimit}" Width="*"/>
                <DataGridTextColumn Header="Tarih" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Info Text -->
        <TextBlock Grid.Row="8" Text="Yeni parametre eklendiğinde mevcut olanlar güncellenir." 
                   FontStyle="Italic" Foreground="Gray" HorizontalAlignment="Center"/>
    </Grid>
</Window>