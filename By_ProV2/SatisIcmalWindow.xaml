<Window x:Class="By_ProV2.SatisIcmalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Süt Satış İcmali"
        Height="250" Width="1000"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Logo.png"
        Background="#F9F9F9">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🟦 Başlık -->
        <Border Background="#2C3E50" CornerRadius="6" Padding="12" Margin="0,0,0,15">
            <TextBlock Text="SÜT SATIŞ İCMALİ"
                       Foreground="White"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- 🔍 Arama Paneli -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="Tarih Aralığı:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                <DatePicker x:Name="dpStartDate" Width="120" Height="25" Margin="0,0,10,0"/>
                <TextBlock Text="ile" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="dpEndDate" Width="120" Height="25" Margin="0,0,15,0"/>
                <TextBlock Text="Cari Kodu:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBox x:Name="txtCariKodu" Width="150" Height="25" Margin="0,0,5,0" KeyDown="TxtCariKodu_KeyDown"/>
                <Button x:Name="btnCariListe" Content="🔍" Width="25" Height="25" Margin="0,0,10,0" Click="BtnCariListe_Click" ToolTip="Cari Listesi"/>
                <Button x:Name="btnSorgula" Content="Raporu Görüntüle" Width="150" Height="25" Margin="0,0,10,0" Click="BtnSorgula_Click"/>
                <Button x:Name="btnTumunuListele" Content="Tüm Carileri Listele" Width="150" Height="25" Click="BtnTumunuListele_Click"/>
            </StackPanel>
            <Popup x:Name="popupCariListesi" PlacementTarget="{Binding ElementName=txtCariKodu}" Placement="Bottom" StaysOpen="False" MaxHeight="250" AllowsTransparency="True">
                <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBox x:Name="txtCariAra" Grid.Row="0" Margin="5" Padding="3" 
                                 TextChanged="TxtCariAra_TextChanged" 
                                 KeyDown="TxtCariAra_KeyDown"
                                 HorizontalAlignment="Stretch" 
                                 VerticalAlignment="Top"
                                 Width="390" Height="25" 
                                 HorizontalContentAlignment="Left" 
                                 VerticalContentAlignment="Center"/>
                        <ScrollViewer Grid.Row="1" MaxHeight="200" VerticalScrollBarVisibility="Auto">
                            <ListBox x:Name="lstCariListesi" DisplayMemberPath="DisplayText"
                                     Width="400" Height="Auto"
                                     SelectionChanged="LstCariListesi_SelectionChanged"
                                     MouseDoubleClick="LstCariListesi_MouseDoubleClick"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Popup>
        </Grid>
    </Grid>
</Window>